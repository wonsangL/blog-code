#chapter01
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/hikari?serverTimezone=UTC
    username: root
    password: mysqlpassword
    register-mbeans: true
    pool-name: hikari
    minimum-idle: 0

spring.jmx.enabled: true

logging:
  level:
    root: info
    com.zaxxer.hikari.pool: debug
